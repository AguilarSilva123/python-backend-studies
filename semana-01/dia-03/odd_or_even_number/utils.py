def is_even(number):
    """
        Check if a number is even.

        Args:
            number (int) : Number to check

        Returns:
            bool: True if even or false is otherwise
    """
    return number%2 == 0